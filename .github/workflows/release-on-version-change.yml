name: Release on Version Change

on:
  push:
    branches: [main]
    paths:
      - "package.json"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch tags
        run: git fetch --tags

      - name: Read version from package.json
        id: ver
        run: |
          VERSION=$(jq -r .version package.json)
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "tag=v$VERSION" >> $GITHUB_OUTPUT

      - name: Gate: only run for >= 1.0.0
        id: gate
        run: |
          MAJOR="${VERSION%%.*}"
          if [ "$MAJOR" -lt 1 ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "Pre-1.0.0 version ($VERSION). Skipping tag/release."
          else
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "Version $VERSION is eligible for tag/release."
          fi

      - name: Check if tag exists
        if: steps.gate.outputs.skip != 'true'
        id: tag_exists
        run: |
          if git rev-parse "${{ steps.ver.outputs.tag }}" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Stop if tag already exists
        if: steps.gate.outputs.skip != 'true' && steps.tag_exists.outputs.exists == 'true'
        run: |
          echo "Tag already exists. Skipping."
          exit 0

      - name: Create and push tag
        if: steps.gate.outputs.skip != 'true' && steps.tag_exists.outputs.exists != 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag "v${VERSION}"
          git push origin "v${VERSION}"

      - name: Extract and format release notes (strip version heading)
        if: steps.gate.outputs.skip != 'true' && steps.tag_exists.outputs.exists != 'true'
        run: |
          VER="${VERSION}"

          awk -v ver="$VER" '
            BEGIN {found=0}
            $0 ~ "^## \\[" ver "\\]" {found=1; next}
            found==1 && $0 ~ "^## \\[" {exit}
            found==1 {print}
          ' CHANGELOG.md > release-notes.md

          if [ ! -s release-notes.md ]; then
            echo "Changes" > release-notes.md
            echo "" >> release-notes.md
            echo "- Automated release for version $VER." >> release-notes.md
          fi

      - name: Create GitHub Release
        if: steps.gate.outputs.skip != 'true' && steps.tag_exists.outputs.exists != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "v${VERSION}" \
            --title "IndicatorEngine ${VERSION}" \
            --notes-file release-notes.md